/**
 * ShowcaseController — Gerencia a navegação e interação do Kitchen Sink / Showcase de Componentes
 */
class ShowcaseController extends Controller {
    execute() {
        new ShowcaseView();
    }
}
